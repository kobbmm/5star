// ไฟล์ config.js สำหรับการตั้งค่าทั่วไปของแอปพลิเคชัน
module.exports = {
  // ตั้งค่าสำหรับหน้าที่ใช้ useSearchParams
  dynamicPages: [
    '/reset-password',
    '/email-verified'
  ],
  // ตั้งค่าสำหรับ API routes
  dynamicApiRoutes: [
    '/api/chart'
  ]
}; 